import type { Company } from "@/lib/types"

// No mock companies - this system uses only real database companies
export const companies: Company[] = []

// No default company - will be loaded from database
export const defaultCompany = null